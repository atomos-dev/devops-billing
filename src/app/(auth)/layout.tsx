/**
 * Auth layout — standalone layout for login page without sidebar or header.
 */
export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
